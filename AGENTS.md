# Repository Instructions

- When the user asks to commit and push, use plain `git` commands. Do not require or use the GitHub CLI (`gh`) unless the user explicitly asks for GitHub CLI or pull-request operations.
- This application is hosted on Google Cloud. Do not use ChatGPT Sites hosting or deploy through Sites; use the existing Google Cloud build and deployment path.
